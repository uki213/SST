image: node:4.4.7

cache:
  untracked: true
  key: "$CI_BUILD_REF_NAME"
  paths:
  - node_modules/

deploy:
  stage: deploy
  script:
  - npm install
  - npm run build
  artifacts:
    paths:
    - build/
